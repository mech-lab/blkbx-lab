# INK Static Verifier

This directory stays independently deployable from the Rails platform.

- `index.html`: static verifier shell
- `verify.js`: local structure-check placeholder until WASM is attached
- `styles.css`: standalone styling
