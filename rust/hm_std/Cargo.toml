[package]
name = "hm_std"
version = "0.1.0-alpha.2"
edition = "2021"
license = "MIT"

[dependencies]
hm_core = { path = "../hm_core", default-features = false, features = ["std"] }

[lib]
name = "hm_std"
path = "src/lib.rs"
