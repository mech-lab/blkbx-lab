# hybrid-mechlab-rust

Optional Rust acceleration companion for `hybrid-mechlab`.

This package builds and installs the `hm_pyo3` extension module. The base
`hybrid-mechlab` package remains pure Python and NumPy-only.
