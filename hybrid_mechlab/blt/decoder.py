"""Experimental sparse decoder scaffolding outside the BLKBX release surface."""


class BLTDecoder:
    def decode(self, codes, target_hook: str):
        _ = (codes, target_hook)
        raise NotImplementedError("BLTDecoder is an experimental research placeholder.")
